name: Publish to npm

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm-publish
    permissions:
      contents: read
      id-token: write # for OIDC trusted publishing

    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "24.x"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run tests
        run: bun test

      - name: Build the project
        run: bun run build

      - name: Lint package contents (publint)
        run: bunx publint

      - name: Ensure npm supports trusted publishing
        # Trusted publishing (OIDC) requires npm >= 11.5.1
        run: npm install -g npm@latest

      - name: Publish to npm
        # Authentication is done via OIDC trusted publishing, not NPM_TOKEN.
        # provenance is attached automatically by trusted publishing, but the
        # flag is kept explicit for clarity.
        run: npm publish --provenance --access public
        # In Bun, there is no `provenance`, so use npm
        # https://github.com/oven-sh/bun/issues/15601

  changelog:
    needs: publish
    runs-on: ubuntu-latest
    permissions:
      contents: write # for creating releases
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      - name: Create GitHub Release
        run: bunx changelogithub@0.12
        env:
          # Automatically provided by GitHub Actions
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
